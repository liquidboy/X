<UserControl
    x:Class="X.NeonShell.Controls.FlickrLoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:X.NeonShell.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <WebView x:Name="wvLoginRequest" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="50" Visibility="Collapsed" />
        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
            <Button x:Name="butLogin" Content="Login" Click="butLogin_Click"/>
            <Button x:Name="butLogout" Content="Logout" Click="butLogout_Click" Margin="10,0,0,0"/>
        </StackPanel>
        <Button Margin="10,0,0,0" Content="Dashboard" Click="{x:Bind GoPublicCommand}" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</UserControl>
